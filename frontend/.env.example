# Copy this file to .env.local (git-ignored) and fill in real values.
#
# Vite only exposes variables prefixed with VITE_ to the client bundle
# (via import.meta.env.*). No variables are defined yet — add them here
# as they're introduced, and keep this file in sync with what the app
# actually reads.
